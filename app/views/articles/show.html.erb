		<div class="container shadow bg-white rounded p-4 text-center my-5 mb-4">
			<h1><%= @article.title %></h1>
				<p><%= "♥" * @article.stars %></p>
					<h3><%= @article.created_at.strftime("%d %b %Y") %></h3>
				<p><%= @article.content %></p>
				<%= link_to "Edit", edit_article_path(@article.id), class: "btn btn-primary" %>
				<%= link_to "Destroy", article_path(@article.id), method: :delete, class: "btn btn-danger" %>
		</div>

